import React from 'react';
import { render } from 'react-dom';
import Search from '../Search/Search';

const App = (props: any) => {
  return (
    <div>
      <Search />
    </div>
  )
}

export default App;
